package org.tobi29.scapes.engine.utils.shader.expression;

public enum UnaryType {
    INCREMENT_GET,
    DECREMENT_GET,
    GET_INCREMENT,
    GET_DECREMENT,
    POSITIVE,
    NEGATIVE,
    BIT_NOT,
    NOT
}
